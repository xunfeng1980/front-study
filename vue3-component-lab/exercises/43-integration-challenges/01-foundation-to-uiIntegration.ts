export function buildFoundationToUiIntegration() {
  // TODO: return { filters, routes, ui, state }
  return {
    filters: [] as string[],
    routes: [] as string[],
    ui: [] as string[],
    state: [] as string[],
  }
}
