export function buildRuntimeToRealtimeIntegration() {
  // TODO: return { auth, ssr, a11y, realtime }
  return {
    auth: [] as string[],
    ssr: [] as string[],
    a11y: [] as string[],
    realtime: [] as string[],
  }
}
