export function buildKeyShortcutLabel(shortcut: {
  meta?: boolean
  shift?: boolean
  alt?: boolean
  key: string
}) {
  // TODO: join pressed modifiers and key with '+'
  // example: { meta: true, shift: true, key: 'K' } -> 'Meta+Shift+K'
  return ''
}
