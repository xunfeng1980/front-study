export function buildAuthPerfCheckpoint() {
  // TODO: return { imageLoading, authHeader, access, preconnect }
  return {
    imageLoading: '',
    authHeader: '',
    access: false,
    preconnect: false,
  }
}
