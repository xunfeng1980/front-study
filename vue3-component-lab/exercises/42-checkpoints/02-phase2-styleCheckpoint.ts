export function buildStyleCheckpoint(theme: 'light' | 'dark', active: boolean) {
  // TODO: return { tokens, classes, motion }
  return {
    tokens: {} as Record<string, string>,
    classes: '',
    motion: '',
  }
}
