export async function resolveLoaderData<TLoader extends () => Promise<unknown>>(
  loader: TLoader,
) {
  // TODO: return the awaited loader result
  // TODO: make the return type use Awaited<ReturnType<TLoader>>
  return undefined
}
