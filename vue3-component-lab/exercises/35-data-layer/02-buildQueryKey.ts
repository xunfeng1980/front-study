export function buildQueryKey(
  scope: string,
  filters: Record<string, string | number | boolean>,
) {
  // TODO: return a stable array query key
  // TODO: include scope first, and a normalized filters object second
  return []
}
