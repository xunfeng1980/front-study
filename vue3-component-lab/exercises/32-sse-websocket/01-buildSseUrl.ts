export function buildSseUrl(
  baseUrl: string,
  topic: string,
  includeUsage = true,
) {
  // TODO: create a URL from baseUrl
  // TODO: set pathname to `/sse`
  // TODO: write `topic`
  // TODO: write `usage` as `true` or `false`
  return `${baseUrl}/events`
}
