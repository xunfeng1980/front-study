export function buildCallbackUrl(
  baseUrl: string,
  params: { code: string; state: string },
) {
  // TODO: create /auth/callback with URL
  // TODO: append code and state with URLSearchParams
  return baseUrl
}
