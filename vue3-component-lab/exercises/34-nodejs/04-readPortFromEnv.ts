export function readPortFromEnv(
  env: NodeJS.ProcessEnv,
  fallback = 3000,
) {
  // TODO: read env.PORT
  // TODO: parse it as a positive integer
  // TODO: fallback when PORT is missing or invalid
  return fallback
}
