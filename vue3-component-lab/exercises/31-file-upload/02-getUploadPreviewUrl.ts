export function getUploadPreviewUrl(
  createObjectURL: (file: unknown) => string,
  file: unknown | null,
) {
  // TODO: when file is null return ''
  // TODO: otherwise return createObjectURL(file)
  return ''
}
