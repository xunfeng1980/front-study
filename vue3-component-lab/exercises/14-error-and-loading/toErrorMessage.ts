export function toErrorMessage(error: unknown): string {
  // TODO: if error is an Error, return error.message
  // TODO: if error is a string, return it directly
  // TODO: otherwise return `未知错误`
  return 'TODO'
}
