export type Todo = {
  id: number
  title: string
  done: boolean
}

export function findTodoById(todos: Todo[], id: number) {
  // TODO: use find to return the todo with the matching id
  return undefined
}
