export type Task = {
  id: number
  updatedAt: string
}

export function sortTasksByUpdatedAt(tasks: Task[]) {
  // TODO: return a new array sorted by updatedAt from newest to oldest
  // TODO: do not mutate the original tasks array
  return tasks
}
