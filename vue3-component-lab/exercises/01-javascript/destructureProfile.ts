export function destructureProfile(profile: {
  name: string
  role: string
  stats: { tasks: number }
}) {
  // TODO: use object destructuring
  // TODO: return `${name}:${role}:${tasks}`
  return 'TODO'
}
