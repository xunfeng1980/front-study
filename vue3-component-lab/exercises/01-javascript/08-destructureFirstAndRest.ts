export function destructureFirstAndRest(members: Array<{ name: string }>) {
  // TODO: use array destructuring to get the first member and the rest members
  // TODO: return `${first.name}:${rest.length}`
  return 'TODO'
}
