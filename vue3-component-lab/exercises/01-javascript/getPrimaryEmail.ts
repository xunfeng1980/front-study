export function getPrimaryEmail(user: {
  profile?: { contact?: { email?: string | null } }
}) {
  // TODO: use optional chaining + nullish coalescing
  // TODO: default to `unknown@example.com`
  return 'TODO'
}
