export function buildAdminDashboardCapstone() {
  // TODO: return { routes, dataModules, uiModules, qualityGates }
  return {
    routes: [] as string[],
    dataModules: [] as string[],
    uiModules: [] as string[],
    qualityGates: [] as string[],
  }
}
