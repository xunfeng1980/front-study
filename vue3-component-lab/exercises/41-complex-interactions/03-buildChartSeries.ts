export function buildChartSeries(points: Array<{ date: string; open: number; closed: number }>) {
  // TODO: return [{ name: 'open', data }, { name: 'closed', data }]
  // TODO: every data item should look like { x: date, y: value }
  return []
}
