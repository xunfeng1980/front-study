export function buildShortcutLabel(keys: string[], platform: 'mac' | 'windows') {
  // TODO: on mac, render meta as Cmd and alt as Opt
  // TODO: on windows, render meta as Win and alt as Alt
  // TODO: join keys with +
  return ''
}
