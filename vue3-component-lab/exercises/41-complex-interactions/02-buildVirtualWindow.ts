export function buildVirtualWindow(input: {
  itemHeight: number
  viewportHeight: number
  scrollTop: number
  total: number
  overscan: number
}) {
  // TODO: return { start, end }
  // TODO: clamp to 0 and total
  return {
    start: 0,
    end: 0,
  }
}
