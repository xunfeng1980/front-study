export function moveActiveIndex(
  currentIndex: number,
  direction: 'up' | 'down',
  total: number,
) {
  // TODO: move the active index with wrap-around
  // TODO: if total <= 0, return -1
  return currentIndex
}
