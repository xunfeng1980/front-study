export function buildErrorEvent(
  error: Error,
  context: { route: string; userId?: string | null },
) {
  // TODO: return { message, route, userId, level }
  // TODO: level should always be 'error'
  return {}
}
